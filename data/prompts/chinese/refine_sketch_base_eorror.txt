你是一名擅长编写 Lean 4 证明的专家。
你将获得一份 Lean 4 的证明草稿（proof sketch），其中某个子目标存在问题。
你的任务是修复这些问题，并写出一份新的证明草稿。

证明草稿：

{sketch}

已发现的问题：

{error_message}

Lean 提示：

无

规则（必须严格遵守）：

相同证明层级 = 相同缩进：处于同一逻辑层级的所有 tactic 必须使用完全一致的缩进。

字符一致性：统一使用制表符（tabs）或空格（spaces），不要混用。

正确嵌套：子证明的缩进必须比其父证明多一层。

禁止嵌套 have：不要在一个 have 的证明中再写另一个 have。不同的子目标必须使用不同的 have 语句。

所有子目标必须命名：不要创建匿名的 have 语句。

禁止包含任何 import 或 open 语句。

一行 = 一个 have 子目标：不要将一个子目标拆分到多行。

使用正确的 Lean 4 语法和规范：并确保整个证明草稿被包含在三引号代码块中：

...


所有子目标的证明必须使用 sorry：关注结构，而非具体实现。

主目标的证明中禁止使用 sorry：必须通过已有的子目标完成主证明。

绝对禁止在定理声明中使用 sorry。

所有子目标合在一起必须足以证明主目标。

明确表达子目标之间的逻辑依赖关系：确保这些子目标在 Lean 4 中是合理且可证明的。

只修改出错的子目标及其之后的部分：在证明草稿中，所有位于该子目标之前的内容必须保持不变。

要么修改有问题的子目标以修复错误，要么添加新的子目标来补足缺失的数学论证。