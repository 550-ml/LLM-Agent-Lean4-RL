你是一名 Lean 4 专家。你的目标是：在给定的证明草图基础上，结合提供的定理，用 Lean 4 写出完整证明。

证明草图（Proof sketch）：
{sketch}

定理列表（Theorems）：
{all_theorems}

要求（Instructions）：

你可以假设这些定理都是正确的，并且可以在证明中直接使用它们。

不要修改给定的这些定理。

不要为这些定理写证明。

不要修改给定的证明草图中的步骤，只需要在原本是 sorry 的地方应用这些定理把缺口补上。

在你的证明中不要使用 sorry。

不要加入任何 import、def 或 open 之类的语句。

不要在你的回答中重新定义这些已经给出的定理。

对任何子目标，不要从零开始自己构造证明，必须始终使用提供的这些定理来完成。

重要指示：

在任何情况下，都不要对“类型未明确的自然数常量”做除法或减法运算，除非这是定理陈述本身所要求的。
例如：1 / 3、2 / 5、1 - 3 这类写法都不允许出现。
一定要写明类型，并且除非定理需要，尽量避免自然数算术。
描述分数时总是要指明类型，比如写 ((2 : R) / 3) 或 ((2 : Q) / 3)，而不是简单的 2 / 3。
这一点在定理以外的所有地方都必须遵守（定理原始声明本身除外）。

同样地，在任何情况下，都不要对类型为自然数（Nat 或 N）的变量做减法或除法，除非这是定理陈述本身要求的。
比如 (a - b) 或 (a / b)（其中 a b : ℕ）这类写法都不允许。
如果必须做这类运算，必须先把变量转换成合适的类型（如 Int、Rat 或 Real），除非定理本身需要；而在一般情况下应尽量避免在自然数上做算术运算。

你的回答必须是单个 Lean 4 代码块，其中只包含该定理的完整证明（已经填好所有 sorry）。